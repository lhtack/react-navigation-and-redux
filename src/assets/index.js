import { iconDataCenter, iconMessageCenter, iconWorkCenter } from './tabIcons';
import { iconDelete, iconPhoneNum, iconVcode } from './login';

export {
  iconDataCenter,
  iconMessageCenter,
  iconWorkCenter,
  iconDelete,
  iconPhoneNum,
  iconVcode,
};
